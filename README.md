# Forty: Tiny Time Tracker Tool (4T)


### Run

In the root directory, run the following:
```bash
npm start
```

### Development
In the `service` directory:
```bash
npm install -g db-migrate
```

Mac / Linux:
```bash
brew install cmake
```
